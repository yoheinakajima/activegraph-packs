# Generated by scripts/generate_manifests.py; rerun after editing the
# pack (CI verifies the content hash and the declared surface).

[pack]
name = "identity_auth"
version = "0.2.1"
description = "Identity resolution and permission checking. Resolves every source to a Principal (owner/admin/collaborator/external/customer/unknown/blocked). Creates AuthContext per session. Blocks unsafe actions from low-trust principals."
license = "Apache-2.0"

[pack.provenance]
authors = ["Yohei Nakajima"]
authored_by = "human"
generator = ""
source_url = "https://github.com/yoheinakajima/activegraph-packs"
created_at = "2026-07-08T00:00:00Z"

[pack.integrity]
content_hash = "sha256:5605d2878e39de2e02c5ba36bd771b729a6e1dd165beed831480aea71071e893"

[dependencies]
activegraph = ">=1.9,<2.0"
python = ">=3.11"
python-deps = []

[dependencies.packs]
core = ">=0.1"

[surface]
object_types = ["principal", "auth_context", "role", "permission", "delegation"]
relation_types = ["resolves_to", "authenticated_by", "granted_by", "granted_to", "linked_to_entity"]
behaviors = ["principal_resolver", "comm_message_principal_resolver", "auth_context_builder", "principal_entity_linker", "permission_checker"]
tools = ["lookup_principal", "check_principal_permission", "register_principal"]
settings_schema = "IdentitySettings"

[fixtures]
entrypoint = "fixtures/run_fixtures.py"
deterministic = true
