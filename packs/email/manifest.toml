# Generated by scripts/generate_manifests.py; rerun after editing the
# pack (CI verifies the content hash and the declared surface).

[pack]
name = "email"
version = "0.1.2"
description = "Email adapter pack. Translates inbound emails into CommMessage(channel=email). email_ingester handles dedup, threading (In-Reply-To / References), and produces Source + CommMessage + EmailThread. reply_drafter formats EmailDraft. send_approver gates external sends with owner approval (policy: external_write_requires_owner_approval)."
license = "Apache-2.0"

[pack.provenance]
authors = ["Yohei Nakajima"]
authored_by = "human"
generator = ""
source_url = "https://github.com/yoheinakajima/activegraph-packs"
created_at = "2026-07-08T00:00:00Z"

[pack.integrity]
content_hash = "sha256:eb2e7b3c69ee6402622c8dd24220b4d3e68e7e5fe3d4d6bd95472bf66e5281eb"

[dependencies]
activegraph = ">=1.7.1,<2.0"
python = ">=3.11"
python-deps = []

[dependencies.packs]
core = ">=0.1"
communication = ">=0.1"

[surface]
object_types = ["email_message", "email_thread", "email_draft"]
relation_types = ["email_thread_contains", "draft_responds_to", "draft_from_candidate", "email_linked_to_comm_thread"]
behaviors = ["email_ingester", "reply_drafter", "send_approver"]
tools = ["ingest_email", "create_email_response"]
settings_schema = "EmailSettings"

[fixtures]
entrypoint = "fixtures/run_fixtures.py"
deterministic = true
