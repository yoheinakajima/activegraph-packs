# Generated by scripts/generate_manifests.py; rerun after editing the
# pack (CI verifies the content hash and the declared surface).

[pack]
name = "telegram"
version = "0.1.1"
description = "Telegram transport adapter: inbound updates become chat_input (reusing the Chat Pack's conversation machinery); approved outbound replies become policy-checked Tool Gateway capability calls with the bot token injected at execution time."
license = "Apache-2.0"

[pack.provenance]
authors = ["Yohei Nakajima"]
authored_by = "human"
generator = ""
source_url = "https://github.com/yoheinakajima/activegraph-packs"
created_at = "2026-07-08T00:00:00Z"

[pack.integrity]
content_hash = "sha256:1184b281e9c918cf01379f99b6edd11eaf7e2434fecd9d6fd0e71fe13a819f45"

[dependencies]
activegraph = ">=1.5,<2.0"
python = ">=3.11"
python-deps = []

[dependencies.packs]
core = ">=0.1"
communication = ">=0.1"
chat = ">=0.1"

[dependencies.optional-packs]
tool_gateway = ">=0.1"
secrets = ">=0.1"
identity_auth = ">=0.1"

[surface]
object_types = ["telegram_update"]
relation_types = ["ingested_as"]
behaviors = ["telegram_ingester", "telegram_dispatcher"]
tools = ["submit_telegram_update"]
settings_schema = "TelegramSettings"
consumes = ["telegram.send_message"]

[[surface.capabilities]]
provider = "telegram"
capability = "send_message"
risk_class = "low"
credential_ref = "TELEGRAM_BOT_TOKEN"

[fixtures]
entrypoint = "fixtures/run_fixtures.py"
deterministic = true
