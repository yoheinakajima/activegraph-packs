# Generated by scripts/generate_manifests.py; rerun after editing the
# pack (CI verifies the content hash and the declared surface).

[pack]
name = "chat"
version = "0.4.0"
description = "Chat adapter pack. Translates chat input into CommMessage(channel=chat). chat_ingester maps raw {'role': 'user', 'content': '...'} input into CommMessage + ChatSession + ChatTurn. chat_llm_responder assembles context and produces CommResponseCandidate (optionally calling Tool Gateway proxy tools in the native LLM loop). chat_responder delivers the response."
license = "Apache-2.0"

[pack.provenance]
authors = ["Yohei Nakajima"]
authored_by = "human"
generator = ""
source_url = "https://github.com/yoheinakajima/activegraph-packs"
created_at = "2026-07-08T00:00:00Z"

[pack.integrity]
content_hash = "sha256:2c182ce6ef8de2144c008f19c6835029edfa2334f0e5a76c721a724bcd29539a"

[dependencies]
activegraph = ">=1.4,<2.0"
python = ">=3.11"
python-deps = []

[dependencies.packs]
core = ">=0.1"
communication = ">=0.1"

[surface]
object_types = ["chat_input", "chat_session", "chat_turn", "chat_context", "memory_context"]
relation_types = ["session_contains_turn", "turn_from_input", "session_has_thread", "provides_context_for"]
behaviors = ["chat_ingester", "chat_context_assembler", "chat_profile_context", "chat_memory_proposer", "chat_memory_context", "chat_llm_responder", "chat_deflection_responder", "chat_responder"]
tools = ["submit_chat_input", "get_session_turns"]
settings_schema = "ChatSettings"

[fixtures]
entrypoint = "fixtures/run_fixtures.py"
deterministic = true
