# Generated by scripts/generate_manifests.py; rerun after editing the
# pack (CI verifies the content hash and the declared surface).

[pack]
name = "whatsapp"
version = "0.2.0"
description = "WhatsApp Cloud API transport adapter: inbound messages become chat_input (reusing the Chat Pack's conversation machinery); approved outbound replies become policy-checked Tool Gateway capability calls with the access token injected at execution time."
license = "Apache-2.0"

[pack.provenance]
authors = ["Yohei Nakajima"]
authored_by = "human"
generator = ""
source_url = "https://github.com/yoheinakajima/activegraph-packs"
created_at = "2026-07-08T00:00:00Z"

[pack.integrity]
content_hash = "sha256:b4db9c7431e249b53ba06c6ce7554dc6c21bf7e3ce31f2f787ceff1c0ce67228"

[dependencies]
activegraph = ">=1.9,<2.0"
python = ">=3.11"
python-deps = []

[dependencies.packs]
core = ">=0.1"
communication = ">=0.1"
chat = ">=0.1"

[dependencies.optional-packs]
tool_gateway = ">=0.1"
secrets = ">=0.1"
identity_auth = ">=0.1"

[surface]
object_types = ["whatsapp_message"]
relation_types = ["wa_ingested_as"]
behaviors = ["whatsapp_ingester", "whatsapp_dispatcher"]
tools = ["submit_whatsapp_webhook"]
settings_schema = "WhatsAppSettings"
consumes = ["whatsapp.send_message"]

[[surface.capabilities]]
provider = "whatsapp"
capability = "send_message"
risk_class = "low"
credential_ref = "WHATSAPP_ACCESS_TOKEN"
action_class = "R3"

[fixtures]
entrypoint = "fixtures/run_fixtures.py"
deterministic = true
