# Generated by scripts/generate_manifests.py; rerun after editing the
# pack (CI verifies the content hash and the declared surface).

[pack]
name = "tool_gateway"
version = "0.6.0"
description = "Capability execution gateway. All external calls (APIs, MCP, local tools) flow through here for policy checks, credential injection by reference, recording, output sanitization, injection scanning, and result mapping to Core source objects. Tool output reaches models fenced as untrusted external content."
license = "Apache-2.0"

[pack.provenance]
authors = ["Yohei Nakajima"]
authored_by = "human"
generator = ""
source_url = "https://github.com/yoheinakajima/activegraph-packs"
created_at = "2026-07-08T00:00:00Z"

[pack.integrity]
content_hash = "sha256:9fcd457a78fde34066c21d96d2048672253d6f3c9fa683e5876fe5b17f6ae7d0"

[dependencies]
activegraph = ">=1.5,<2.0"
python = ">=3.11"
python-deps = []

[dependencies.packs]
core = ">=0.1"

[dependencies.optional-packs]
secrets = ">=0.1"
identity_auth = ">=0.1"

[surface]
object_types = ["capability_provider", "capability_call", "capability_approval", "capability_denial", "capability_result", "injection_flag"]
relation_types = ["calls", "approved_by", "denied_by", "produces_result", "sourced_as", "flags"]
behaviors = ["call_recorder", "policy_enforcer", "call_executor", "result_sourcer"]
tools = ["execute_capability", "pending_approvals", "approve_capability", "deny_capability"]
settings_schema = "ToolGatewaySettings"

[[surface.capabilities]]
provider = "catalog"
capability = "search"
risk_class = "low"
credential_ref = ""

[[surface.capabilities]]
provider = "web"
capability = "fetch_url"
risk_class = "low"
credential_ref = ""

[fixtures]
entrypoint = "fixtures/run_fixtures.py"
deterministic = true
