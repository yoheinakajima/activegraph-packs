# Generated by scripts/generate_manifests.py; rerun after editing the
# pack (CI verifies the content hash and the declared surface).

[pack]
name = "connector_control"
version = "0.2.0"
description = "Neutral connector surface bindings, authoritative-run adapters, learning deltas, and validated family-native read shapes."
license = "Apache-2.0"

[pack.provenance]
authors = ["Yohei Nakajima"]
authored_by = "human"
generator = ""
source_url = "https://github.com/yoheinakajima/activegraph-packs"
created_at = "2026-07-08T00:00:00Z"

[pack.integrity]
content_hash = "sha256:e2a2fd7e811da5e1f0f6fb2069d3b5a246e7f2743e5585b2bf62797f243245fa"

[dependencies]
activegraph = ">=1.9,<2.0"
python = ">=3.11"
python-deps = []

[surface]
object_types = ["connector_surface_binding", "connector_run_observation", "connector_learning_delta", "connector_native_view", "connector_operational_policy"]
relation_types = []
behaviors = ["seed_connector_operational_policy"]
tools = ["record_connector_binding", "record_connector_run_observation", "record_connector_learning_delta", "record_connector_native_view", "project_connector_control_plane", "project_connector_learning_deltas"]
settings_schema = "ConnectorControlSettings"

[fixtures]
entrypoint = "fixtures/run_fixtures.py"
deterministic = true
