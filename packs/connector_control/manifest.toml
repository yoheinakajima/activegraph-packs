# Generated by scripts/generate_manifests.py; rerun after editing the
# pack (CI verifies the content hash and the declared surface).

[pack]
name = "connector_control"
version = "0.6.0"
description = "Neutral connector surface bindings, authoritative-run adapters, learning deltas, ingestion plans, and validated family-native read shapes."
license = "Apache-2.0"

[pack.provenance]
authors = ["Yohei Nakajima"]
authored_by = "human"
generator = ""
source_url = "https://github.com/yoheinakajima/activegraph-packs"
created_at = "2026-07-08T00:00:00Z"

[pack.integrity]
content_hash = "sha256:7c7d6005f2196065945ff372ce340c3fcc6a6f656e92a203aef17b3dd98ca47f"

[dependencies]
activegraph = ">=1.9,<2.0"
python = ">=3.11"
python-deps = []

[surface]
object_types = ["connector_surface_binding", "connector_run_observation", "connector_learning_delta", "connector_native_view", "connector_operational_policy", "connector_maintenance_request", "connector_ingestion_plan", "external_work_attempt"]
relation_types = []
behaviors = ["seed_connector_operational_policy", "project_source_lifecycle_to_connector_binding", "settle_ingestion_plan_on_terminal_run"]
tools = ["record_connector_binding", "record_connector_run_observation", "record_connector_learning_delta", "record_connector_native_view", "project_connector_control_plane", "project_connector_learning_deltas", "request_connector_refresh", "propose_ingestion_plan", "edit_ingestion_plan", "approve_ingestion_plan", "abandon_ingestion_plan", "execute_ingestion_plan", "project_ingestion_plans"]
settings_schema = "ConnectorControlSettings"

[fixtures]
entrypoint = "fixtures/run_fixtures.py"
deterministic = true
