# Generated by scripts/generate_manifests.py; rerun after editing the
# pack (CI verifies the content hash and the declared surface).

[pack]
name = "gmail"
version = "0.6.0"
description = "Canonical Gmail service connector: budgeted exploration into a service/account profile, bounded replayable backfill, history-watermark polling, provider-neutral evidence, local conversation-family mapping with deterministic hygiene, local draft candidates, reversible draft writes, and R3-forever sends through an injected route adapter (Composio first)."
license = "Apache-2.0"

[pack.provenance]
authors = ["Yohei Nakajima"]
authored_by = "human"
generator = ""
source_url = "https://github.com/yoheinakajima/activegraph-packs"
created_at = "2026-07-08T00:00:00Z"

[pack.integrity]
content_hash = "sha256:767d0d3cae59237cf9eaecf8025aab52c2b2e66eb3dadffce9f64e90a1074cda"

[dependencies]
activegraph = ">=1.9,<2.0"
python = ">=3.11"
python-deps = []

[dependencies.packs]
activity_normalizer = ">=0.1"
semantic_extraction = ">=0.1"
communication = ">=0.1"
usage = ">=0.1"
tool_gateway = ">=0.1"
connector_control = ">=0.1"

[surface]
object_types = ["gmail_sync_run", "gmail_draft_candidate"]
relation_types = ["gmail_sync_call", "gmail_draft_call"]
behaviors = ["gmail_exploration_projector", "gmail_sync_result_ingester", "gmail_draft_result_projector", "gmail_control_plane_adapter", "gmail_control_plane_run_created", "gmail_conversation_family_ready", "gmail_conversation_batch_projector", "gmail_control_plane_learning_settled", "gmail_control_plane_failure"]
tools = ["request_gmail_exploration", "request_gmail_backfill", "propose_gmail_ingestion_plan", "request_gmail_poll", "create_gmail_draft_candidate", "request_gmail_draft_sync", "request_gmail_draft_send", "reprocess_gmail_evidence"]
settings_schema = "GmailSettings"

[[surface.capabilities]]
provider = "gmail"
capability = "drafts.create"
risk_class = "medium"
credential_ref = ""
action_class = "R2"

[[surface.capabilities]]
provider = "gmail"
capability = "drafts.send"
risk_class = "high"
credential_ref = ""
action_class = "R3"

[[surface.capabilities]]
provider = "gmail"
capability = "history.list"
risk_class = "low"
credential_ref = ""
action_class = "R0"

[[surface.capabilities]]
provider = "gmail"
capability = "labels.list"
risk_class = "low"
credential_ref = ""
action_class = "R0"

[[surface.capabilities]]
provider = "gmail"
capability = "messages.fetch"
risk_class = "low"
credential_ref = ""
action_class = "R0"

[[surface.capabilities]]
provider = "gmail"
capability = "messages.get"
risk_class = "low"
credential_ref = ""
action_class = "R0"

[[surface.capabilities]]
provider = "gmail"
capability = "profile.get"
risk_class = "low"
credential_ref = ""
action_class = "R0"

[fixtures]
entrypoint = "fixtures/run_fixtures.py"
deterministic = true
