# Generated by scripts/generate_manifests.py; rerun after editing the
# pack (CI verifies the content hash and the declared surface).

[pack]
name = "schedule"
version = "0.2.0"
description = "Graph-native scheduling: schedules declare when to fire and what to emit; ticks are event-first records of due moments; a host driver injects timestamps at the edge. No clock, no thread, no loop in the pack — proactive behavior without an orchestrator."
license = "Apache-2.0"

[pack.provenance]
authors = ["Yohei Nakajima"]
authored_by = "human"
generator = ""
source_url = "https://github.com/yoheinakajima/activegraph-packs"
created_at = "2026-07-08T00:00:00Z"

[pack.integrity]
content_hash = "sha256:a46b66210d8f67a3da7ddf500ce3eb8e16e36a5ce49e80677144f5df69d850da"

[dependencies]
activegraph = ">=1.9,<2.0"
python = ">=3.11"
python-deps = []

[dependencies.packs]
core = ">=0.1"

[dependencies.optional-packs]
communication = ">=0.1"
tool_gateway = ">=0.1"

[surface]
object_types = ["schedule", "schedule_tick", "heartbeat"]
relation_types = ["tick_of", "emitted_by"]
behaviors = ["tick_router", "schedule_bookkeeper"]
tools = ["create_schedule", "emit_due_ticks", "list_due_schedules", "set_schedule_enabled"]
settings_schema = "ScheduleSettings"

[[surface.capabilities]]
provider = "schedule"
capability = "create_reminder"
risk_class = "low"
credential_ref = ""
action_class = "R3"

[fixtures]
entrypoint = "fixtures/run_fixtures.py"
deterministic = true
