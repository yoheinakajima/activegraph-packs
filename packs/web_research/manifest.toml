# Generated by scripts/generate_manifests.py; rerun after editing the
# pack (CI verifies the content hash and the declared surface).

[pack]
name = "web_research"
version = "0.5.0"
description = "Owner-consented adaptive research campaigns: seed queries derive only from confirmed subject facts and run as strikeable surfaces of an approved ingestion plan; bounded rounds record a follow-up frontier with lineage through a provider-neutral search adapter, scope expansion pauses for approval, deterministic budgets stop the campaign with a recorded reason, and every discovered page ingests through the governed public-presence gateway."
license = "Apache-2.0"

[pack.provenance]
authors = ["Yohei Nakajima"]
authored_by = "human"
generator = ""
source_url = "https://github.com/yoheinakajima/activegraph-packs"
created_at = "2026-07-08T00:00:00Z"

[pack.integrity]
content_hash = "sha256:4105f7078cbee2b1d714edb47949d256a9c9cc10257fde42766f7bd72c8c43f7"

[dependencies]
activegraph = ">=1.9,<2.0"
python = ">=3.11"
python-deps = []

[dependencies.packs]
tool_gateway = ">=0.1"
connector_control = ">=0.1"
subject_profile = ">=0.1"
activity_normalizer = ">=0.1"

[surface]
object_types = ["web_research_run", "research_query", "research_scope_amendment"]
relation_types = []
behaviors = []
tools = []
settings_schema = "WebResearchSettings"

[fixtures]
entrypoint = "fixtures/run_fixtures.py"
deterministic = true
