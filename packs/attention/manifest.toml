# Generated by scripts/generate_manifests.py; rerun after editing the
# pack (CI verifies the content hash and the declared surface).

[pack]
name = "attention"
version = "0.1.0"
description = "Semantic engagement observations and bounded interaction batches, without choosing the future importance/trust vector representation."
license = "Apache-2.0"

[pack.provenance]
authors = ["Yohei Nakajima"]
authored_by = "human"
generator = ""
source_url = "https://github.com/yoheinakajima/activegraph-packs"
created_at = "2026-07-08T00:00:00Z"

[pack.integrity]
content_hash = "sha256:11d18ad1430193c02d72566011bb383dbce7a1a6c8a14dbae054a52b8f9baf6a"

[dependencies]
activegraph = ">=1.9,<2.0"
python = ">=3.11"
python-deps = []

[surface]
object_types = ["attention_observation", "interaction_batch"]
relation_types = []
behaviors = []
tools = ["record_attention_observation", "record_interaction_batch"]
settings_schema = "AttentionSettings"

[fixtures]
entrypoint = "fixtures/run_fixtures.py"
deterministic = true
