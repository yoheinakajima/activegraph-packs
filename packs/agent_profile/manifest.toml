# Generated by scripts/generate_manifests.py; rerun after editing the
# pack (CI verifies the content hash and the declared surface).

[pack]
name = "agent_profile"
version = "0.1.1"
description = "Agent identity, goals, standing instructions, and owner preferences. Context is behavior-scoped (not a global blob) — filtered by channel and audience role. ProfileContextRequest → profile_context_provider → ProfileContextView."
license = "Apache-2.0"

[pack.provenance]
authors = ["Yohei Nakajima"]
authored_by = "human"
generator = ""
source_url = "https://github.com/yoheinakajima/activegraph-packs"
created_at = "2026-07-08T00:00:00Z"

[pack.integrity]
content_hash = "sha256:4a4498e554e13b0c77d651ed8e51bfe200bfdd32f83ad72a9bf362caa8121fce"

[dependencies]
activegraph = ">=1.7.1,<2.0"
python = ">=3.11"
python-deps = []

[dependencies.packs]
core = ">=0.1"

[surface]
object_types = ["agent_profile", "goal", "standing_instruction", "personality_profile", "owner_preference", "profile_context_request", "profile_context_view"]
relation_types = ["owns_goal", "owns_instruction", "owns_preference", "fulfilled_by_profile", "triggers_context_for"]
behaviors = ["profile_registry_recorder", "goal_registry_recorder", "instruction_registry_recorder", "personality_registry_recorder", "preference_registry_recorder", "profile_context_trigger", "frame_context_trigger", "profile_context_provider"]
tools = ["register_profile", "request_profile_context"]
settings_schema = "AgentProfileSettings"

[fixtures]
entrypoint = "fixtures/run_fixtures.py"
deterministic = true
