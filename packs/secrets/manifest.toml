# Generated by scripts/generate_manifests.py; rerun after editing the
# pack (CI verifies the content hash and the declared surface).

[pack]
name = "secrets"
version = "0.2.0"
description = "Credential reference management and usage auditing. Secrets never enter model context — CredentialRef contains names only. Actual values resolved from environment variables at execution time."
license = "Apache-2.0"

[pack.provenance]
authors = ["Yohei Nakajima"]
authored_by = "human"
generator = ""
source_url = "https://github.com/yoheinakajima/activegraph-packs"
created_at = "2026-07-08T00:00:00Z"

[pack.integrity]
content_hash = "sha256:1eea6886cce215c4ebc1ebba1315d41184345bad686bb513857cfe0600db2613"

[dependencies]
activegraph = ">=1.9,<2.0"
python = ">=3.11"
python-deps = []

[dependencies.packs]
core = ">=0.1"

[dependencies.optional-packs]
tool_gateway = ">=0.1"

[surface]
object_types = ["secret_scope", "credential_ref", "secret_usage_event", "credential_rotation_event"]
relation_types = ["credential_used_in", "governed_by"]
behaviors = ["secret_usage_recorder", "credential_resolution_recorder"]
tools = ["resolve_credential"]
settings_schema = "SecretsSettings"

[fixtures]
entrypoint = "fixtures/run_fixtures.py"
deterministic = true
