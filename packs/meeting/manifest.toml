# Generated by scripts/generate_manifests.py; rerun after editing the
# pack (CI verifies the content hash and the declared surface).

[pack]
name = "meeting"
version = "0.1.2"
description = "Meeting ingestion and processing: transcript parsing (structured and plain text), decision extraction, action item extraction with Core task creation, and meeting summarization. Provides 5 meeting-domain object types."
license = "Apache-2.0"

[pack.provenance]
authors = ["Yohei Nakajima"]
authored_by = "human"
generator = ""
source_url = "https://github.com/yoheinakajima/activegraph-packs"
created_at = "2026-07-08T00:00:00Z"

[pack.integrity]
content_hash = "sha256:215419eebde2a3386ff8d0a81de8868957a0a08a282d23ba9c8864600408037b"

[dependencies]
activegraph = ">=1.4,<2.0"
python = ">=3.11"
python-deps = []

[dependencies.packs]
core = ">=0.1"

[surface]
object_types = ["meeting", "transcript_segment", "meeting_decision", "meeting_action_item", "meeting_note"]
relation_types = ["segment_of", "decision_in", "action_item_in", "note_for", "decision_from_segment", "action_item_from_segment", "action_creates_task"]
behaviors = ["transcript_ingester", "decision_extractor", "action_item_extractor", "meeting_summarizer"]
tools = ["ingest_transcript", "create_meeting", "add_decision", "add_action_item"]
settings_schema = "MeetingSettings"

[fixtures]
entrypoint = "fixtures/run_fixtures.py"
deterministic = true
