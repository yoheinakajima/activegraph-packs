# Generated by scripts/generate_manifests.py; rerun after editing the
# pack (CI verifies the content hash and the declared surface).

[pack]
name = "subject_profile"
version = "0.4.0"
description = "Evidence-backed, explicitly reviewed subject facts with contradiction and supersession lifecycles, the owner alias-set projection, and importance seeding from confirmed facts."
license = "Apache-2.0"

[pack.provenance]
authors = ["Yohei Nakajima"]
authored_by = "human"
generator = ""
source_url = "https://github.com/yoheinakajima/activegraph-packs"
created_at = "2026-07-08T00:00:00Z"

[pack.integrity]
content_hash = "sha256:6bd0dd33bc7aa5ec7f47b066c7d46c63868bcbcd100f13ece9081e482d5734ef"

[dependencies]
activegraph = ">=1.9,<2.0"
python = ">=3.11"
python-deps = []

[dependencies.packs]
activity_normalizer = ">=0.1"

[dependencies.optional-packs]
semantic_extraction = ">=0.1"
entity = ">=0.1"
memory_gateway = ">=0.1"

[surface]
object_types = ["subject_fact_verdict", "subject_fact", "subject_contradiction"]
relation_types = ["verdict_for_profile_candidate", "promoted_from_profile_candidate", "subject_fact_grounded_in", "subject_fact_supersedes", "contradiction_involves"]
behaviors = ["apply_subject_fact_verdict", "seed_importance_from_confirmed_fact"]
tools = ["review_subject_fact", "forget_subject_fact"]
settings_schema = "SubjectProfileSettings"

[fixtures]
entrypoint = "fixtures/run_fixtures.py"
deterministic = true
