# Generated by scripts/generate_manifests.py; rerun after editing the
# pack (CI verifies the content hash and the declared surface).

[pack]
name = "communication"
version = "0.2.1"
description = "Channel-neutral communication semantic layer. Provides CommThread, CommMessage, CommIntent, CommResponseCandidate, CommParticipant. intent_detector classifies message intent. thread_tracker maintains thread state. response_dispatcher routes approved responses to channel adapters."
license = "Apache-2.0"

[pack.provenance]
authors = ["Yohei Nakajima"]
authored_by = "human"
generator = ""
source_url = "https://github.com/yoheinakajima/activegraph-packs"
created_at = "2026-07-08T00:00:00Z"

[pack.integrity]
content_hash = "sha256:8e22128c0086216df4ace75a44a53da83fee4761028fb66332b803e750bd6bd9"

[dependencies]
activegraph = ">=1.7.1,<2.0"
python = ">=3.11"
python-deps = []

[dependencies.packs]
core = ">=0.1"

[surface]
object_types = ["comm_thread", "comm_message", "comm_intent", "comm_response_candidate", "comm_participant"]
relation_types = ["thread_contains", "intent_of", "response_to", "participates_in", "derived_from_source_comm", "dispatched_to", "delivers", "fulfills_intent"]
behaviors = ["intent_detector", "intent_router", "thread_tracker", "response_dispatcher"]
tools = ["create_comm_message", "create_response_candidate", "approve_response"]
settings_schema = "CommunicationSettings"

[fixtures]
entrypoint = "fixtures/run_fixtures.py"
deterministic = true
