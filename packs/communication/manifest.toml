# Generated by scripts/generate_manifests.py; rerun after editing the
# pack (CI verifies the content hash and the declared surface).

[pack]
name = "communication"
version = "0.4.0"
description = "Channel-neutral communication semantic layer. Provides the durable conversation-family contract with exact-span staged interpretation, plus the legacy CommThread/CommMessage action layer. Channel adapters own service mapping; semantic extraction owns bounded model execution."
license = "Apache-2.0"

[pack.provenance]
authors = ["Yohei Nakajima"]
authored_by = "human"
generator = ""
source_url = "https://github.com/yoheinakajima/activegraph-packs"
created_at = "2026-07-08T00:00:00Z"

[pack.integrity]
content_hash = "sha256:e8b0c51615f8151f09deaf9aa67b6f83aa28286f86559cd528c5fd43ef84d093"

[dependencies]
activegraph = ">=1.9,<2.0"
python = ">=3.11"
python-deps = []

[dependencies.packs]
core = ">=0.1"
semantic_extraction = ">=0.1"
entity = ">=0.1"

[surface]
object_types = ["comm_thread", "comm_message", "comm_intent", "comm_response_candidate", "comm_participant", "conversation_thread", "conversation_message", "conversation_participant", "conversation_interpretation_run"]
relation_types = ["thread_contains", "intent_of", "response_to", "participates_in", "derived_from_source_comm", "dispatched_to", "delivers", "fulfills_intent", "conversation_contains", "conversation_has_participant", "conversation_message_from_evidence", "conversation_participant_mention"]
behaviors = ["intent_detector", "intent_router", "thread_tracker", "response_dispatcher", "settle_conversation_interpretation", "project_conversation_followup_candidate"]
tools = ["create_comm_message", "create_response_candidate", "approve_response"]
settings_schema = "CommunicationSettings"

[fixtures]
entrypoint = "fixtures/run_fixtures.py"
deterministic = true
