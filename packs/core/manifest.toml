# Generated by scripts/generate_manifests.py; rerun after editing the
# pack (CI verifies the content hash and the declared surface).

[pack]
name = "core"
version = "0.1.2"
description = "Universal primitive substrate for all ActiveGraph packs. Provides 7 object types (source, observation, task, action, artifact, memory_candidate, evaluation) and 7 relation types. All behaviors are deterministic — no LLM or API key required."
license = "Apache-2.0"

[pack.provenance]
authors = ["Yohei Nakajima"]
authored_by = "human"
generator = ""
source_url = "https://github.com/yoheinakajima/activegraph-packs"
created_at = "2026-07-08T00:00:00Z"

[pack.integrity]
content_hash = "sha256:881204a11e490fc7168505c39890e511d57b10200363356b613ac3138e6031c7"

[dependencies]
activegraph = ">=1.4,<2.0"
python = ">=3.11"
python-deps = []

[surface]
object_types = ["source", "observation", "task", "action", "artifact", "memory_candidate", "evaluation"]
relation_types = ["grounds", "produces", "executes", "generates", "proposes", "evaluates", "derived_from", "derived_from_source"]
behaviors = ["observation_extractor", "task_linker", "memory_candidate_proposer"]
tools = []
settings_schema = "CoreSettings"

[fixtures]
entrypoint = "fixtures/run_fixtures.py"
deterministic = true
