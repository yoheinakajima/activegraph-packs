# Generated by scripts/generate_manifests.py; rerun after editing the
# pack (CI verifies the content hash and the declared surface).

[pack]
name = "codebase"
version = "0.1.2"
description = "Codebase tracking: repo ingestion, issue/PR tracking, ADR extraction, code change summarization, dependency auditing. Provides 9 object types for engineering workflow visibility."
license = "Apache-2.0"

[pack.provenance]
authors = ["Yohei Nakajima"]
authored_by = "human"
generator = ""
source_url = "https://github.com/yoheinakajima/activegraph-packs"
created_at = "2026-07-08T00:00:00Z"

[pack.integrity]
content_hash = "sha256:bc4360637b9f666173b7ce3a3cafeed1fff7edbe604ec8faede2517a4bc0fb0c"

[dependencies]
activegraph = ">=1.5,<2.0"
python = ">=3.11"
python-deps = []

[dependencies.packs]
core = ">=0.1"

[surface]
object_types = ["repo", "code_file", "code_function", "dependency", "issue", "pull_request", "architecture_decision", "code_change", "test_result"]
relation_types = ["file_in_repo", "function_in_file", "repo_depends_on", "issue_in_repo", "pr_in_repo", "adr_in_repo", "change_in_repo", "test_for_repo"]
behaviors = ["repo_ingester", "issue_tracker", "adr_extractor", "change_summarizer", "dependency_auditor"]
tools = ["ingest_github_webhook", "ingest_repo_file", "create_repo", "create_issue"]
settings_schema = "CodebaseSettings"

[fixtures]
entrypoint = "fixtures/run_fixtures.py"
deterministic = true
