# Generated by scripts/generate_manifests.py; rerun after editing the
# pack (CI verifies the content hash and the declared surface).

[pack]
name = "subject_synthesis"
version = "0.5.0"
description = "Bounded, provider-gated comprehension synthesis: structured identity candidates lifted from confirmed prose and a curated, evidence-cited project slate — verdict-gated, receipted, with the deterministic derivation left untouched as the zero-key floor. ADR 0047 adds understanding affordances, source lenses over one versioned working understanding with support-vs-context lineage, and a governed dynamic coordinator with bounded drill-downs."
license = "Apache-2.0"

[pack.provenance]
authors = ["Yohei Nakajima"]
authored_by = "human"
generator = ""
source_url = "https://github.com/yoheinakajima/activegraph-packs"
created_at = "2026-07-08T00:00:00Z"

[pack.integrity]
content_hash = "sha256:25c7fd47a8df9e5c263d812346007773a1545f454f149773d54987187997dd8a"

[dependencies]
activegraph = ">=1.9,<2.0"
python = ">=3.11"
python-deps = []

[dependencies.packs]
activity_normalizer = ">=0.1"
subject_profile = ">=0.1"
projects = ">=0.1"

[dependencies.optional-packs]
entity = ">=0.1"
connector_control = ">=0.1"
llm_provider = ">=0.1"

[surface]
object_types = ["subject_synthesis_request", "subject_synthesis_run", "comprehension_request", "source_item_summary", "comprehension_aggregate", "setup_draft", "setup_draft_item", "information_access_hint", "understanding_delta", "source_lens", "working_understanding", "reinterpretation_request", "comprehension_campaign", "coordinator_move", "owner_question", "evidence_drill_down"]
relation_types = []
behaviors = []
tools = []
settings_schema = "SubjectSynthesisSettings"

[fixtures]
entrypoint = "fixtures/run_fixtures.py"
deterministic = true
