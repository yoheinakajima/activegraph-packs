# Generated by scripts/generate_manifests.py; rerun after editing the
# pack (CI verifies the content hash and the declared surface).

[pack]
name = "team_ops"
version = "0.1.2"
description = "Team project management: task triage, assignment, milestone tracking, workload estimation, and completion verification. Extends Core task with 6 project management object types."
license = "Apache-2.0"

[pack.provenance]
authors = ["Yohei Nakajima"]
authored_by = "human"
generator = ""
source_url = "https://github.com/yoheinakajima/activegraph-packs"
created_at = "2026-07-08T00:00:00Z"

[pack.integrity]
content_hash = "sha256:a3643a43fe42ff4781dcd15385eb992f7843beb0bfbece044c97d6d944b3d8d5"

[dependencies]
activegraph = ">=1.7,<2.0"
python = ">=3.11"
python-deps = []

[dependencies.packs]
core = ">=0.1"

[surface]
object_types = ["project", "assignment", "milestone", "workload_estimate", "completion_evidence", "review_request"]
relation_types = ["assigned_to", "depends_on", "part_of_milestone", "part_of_project", "evidence_for", "review_of", "workload_for"]
behaviors = ["task_triager", "assignment_suggester", "milestone_tracker", "completion_verifier"]
tools = ["create_project", "create_milestone", "submit_task_candidate", "assign_task", "mark_task_done"]
settings_schema = "TeamOpsSettings"

[fixtures]
entrypoint = "fixtures/run_fixtures.py"
deterministic = true
