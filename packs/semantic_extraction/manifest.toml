# Generated by scripts/generate_manifests.py; rerun after editing the
# pack (CI verifies the content hash and the declared surface).

[pack]
name = "semantic_extraction"
version = "0.1.0"
description = "Shared annotation layer: typed source-anchored annotations under one provenance envelope, cache-identified deterministic extraction, first-class coverage, and per-domain candidate projectors."
license = "Apache-2.0"

[pack.provenance]
authors = ["Yohei Nakajima"]
authored_by = "human"
generator = ""
source_url = "https://github.com/yoheinakajima/activegraph-packs"
created_at = "2026-07-08T00:00:00Z"

[pack.integrity]
content_hash = "sha256:bf581d5d7efb4e6b8e0a2f04c2c24eb22688c0f1674f0ec06b0c6a88692fe1c0"

[dependencies]
activegraph = ">=1.9,<2.0"
python = ">=3.11"
python-deps = []

[dependencies.packs]
activity_normalizer = ">=0.1"

[dependencies.optional-packs]
core = ">=0.1"
memory_gateway = ">=0.1"

[surface]
object_types = ["semantic_annotation", "extraction_run", "extraction_coverage", "extraction_profile", "annotation_extractor_state"]
relation_types = ["annotation_for", "produced_annotation", "run_for", "coverage_for", "projected_from_annotation"]
behaviors = ["seed_extraction_profile", "annotate_evidence", "project_profile_candidates", "project_memory_candidates"]
tools = ["extract_annotations", "update_extraction_profile", "invalidate_annotation_extractor", "annotation_coverage"]
settings_schema = "SemanticExtractionSettings"

[fixtures]
entrypoint = "fixtures/run_fixtures.py"
deterministic = true
