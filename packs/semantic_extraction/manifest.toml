# Generated by scripts/generate_manifests.py; rerun after editing the
# pack (CI verifies the content hash and the declared surface).

[pack]
name = "semantic_extraction"
version = "0.2.0"
description = "Shared annotation layer: typed source-anchored annotations under one provenance envelope, cache-identified deterministic extraction, first-class coverage, generic exact-span extraction requests, and per-domain candidate projectors."
license = "Apache-2.0"

[pack.provenance]
authors = ["Yohei Nakajima"]
authored_by = "human"
generator = ""
source_url = "https://github.com/yoheinakajima/activegraph-packs"
created_at = "2026-07-08T00:00:00Z"

[pack.integrity]
content_hash = "sha256:f0d45829d0a4f7ae507800e7b875f2d0e6c2a4589bed49dbf0fe5b6209126fe1"

[dependencies]
activegraph = ">=1.9,<2.0"
python = ">=3.11"
python-deps = []

[dependencies.packs]
activity_normalizer = ">=0.1"

[dependencies.optional-packs]
core = ">=0.1"
memory_gateway = ">=0.1"

[surface]
object_types = ["semantic_annotation", "extraction_run", "extraction_coverage", "selection_extraction_request", "extraction_profile", "annotation_extractor_state", "extractor_promotion_evidence"]
relation_types = ["annotation_for", "produced_annotation", "run_for", "coverage_for", "projected_from_annotation"]
behaviors = ["seed_extraction_profile", "annotate_evidence", "extract_selected_evidence", "project_profile_candidates", "project_memory_candidates"]
tools = ["extract_annotations", "update_extraction_profile", "invalidate_annotation_extractor", "annotation_coverage", "run_extractor_trial", "promote_llm_extractor"]
settings_schema = "SemanticExtractionSettings"

[fixtures]
entrypoint = "fixtures/run_fixtures.py"
deterministic = true
