# Generated by scripts/generate_manifests.py; rerun after editing the
# pack (CI verifies the content hash and the declared surface).

[pack]
name = "semantic_extraction"
version = "0.3.0"
description = "Shared annotation layer: typed source-anchored annotations under one provenance envelope, cache-identified deterministic extraction, first-class coverage, exact-span requests, per-domain candidate projectors, and guarded dynamic-to-deterministic procedures."
license = "Apache-2.0"

[pack.provenance]
authors = ["Yohei Nakajima"]
authored_by = "human"
generator = ""
source_url = "https://github.com/yoheinakajima/activegraph-packs"
created_at = "2026-07-08T00:00:00Z"

[pack.integrity]
content_hash = "sha256:112a80256a6eab83d4ea29b5cee07e2bbd0c296104f12ed42b690dadb79cef8c"

[dependencies]
activegraph = ">=1.9,<2.0"
python = ">=3.11"
python-deps = []

[dependencies.packs]
activity_normalizer = ">=0.1"

[dependencies.optional-packs]
core = ">=0.1"
memory_gateway = ">=0.1"

[surface]
object_types = ["semantic_annotation", "extraction_run", "extraction_coverage", "selection_extraction_request", "extraction_profile", "annotation_extractor_state", "extractor_promotion_evidence", "procedure_guard", "procedure_candidate", "procedure_evaluation", "procedure_deoptimization"]
relation_types = ["annotation_for", "produced_annotation", "run_for", "coverage_for", "projected_from_annotation", "procedure_has_guard", "procedure_evaluated_by", "procedure_deoptimized_by"]
behaviors = ["seed_extraction_profile", "annotate_evidence", "extract_selected_evidence", "project_profile_candidates", "project_memory_candidates"]
tools = ["extract_annotations", "update_extraction_profile", "invalidate_annotation_extractor", "annotation_coverage", "run_extractor_trial", "promote_llm_extractor", "synthesize_request_procedure", "promote_request_procedure"]
settings_schema = "SemanticExtractionSettings"

[fixtures]
entrypoint = "fixtures/run_fixtures.py"
deterministic = true
