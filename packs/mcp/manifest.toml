# Generated by scripts/generate_manifests.py; rerun after editing the
# pack (CI verifies the content hash and the declared surface).

[pack]
name = "mcp"
version = "0.2.1"
description = "Bidirectional MCP: outbound servers' tools become governed Tool Gateway capabilities (approval-required by default); inbound, the assistant is itself an MCP server exposing chat, subject-scoped memory search, and selected skills — token-authenticated, role-gated by graph-native exposure rules, fully audited."
license = "Apache-2.0"

[pack.provenance]
authors = ["Yohei Nakajima"]
authored_by = "human"
generator = ""
source_url = "https://github.com/yoheinakajima/activegraph-packs"
created_at = "2026-07-08T00:00:00Z"

[pack.integrity]
content_hash = "sha256:2520179ea111b96d62b16cc638dd380235ca9df4de94ac4c16ab9e2e455fba63"

[dependencies]
activegraph = ">=1.4,<2.0"
python = ">=3.11"
python-deps = []

[dependencies.packs]
core = ">=0.1"
tool_gateway = ">=0.1"

[dependencies.optional-packs]
identity_auth = ">=0.1"
chat = ">=0.1"
memory_gateway = ">=0.1"
secrets = ">=0.1"

[surface]
object_types = ["mcp_server", "mcp_exposure", "mcp_access"]
relation_types = []
behaviors = []
tools = []
settings_schema = "MCPSettings"

[[surface.capabilities]]
provider = "mcp"
capability = "set_exposure"
risk_class = "high"
credential_ref = ""

[fixtures]
entrypoint = "fixtures/run_fixtures.py"
deterministic = true
