# Generated by scripts/generate_manifests.py; rerun after editing the
# pack (CI verifies the content hash and the declared surface).

[pack]
name = "mcp"
version = "0.3.0"
description = "Bidirectional MCP: outbound servers' tools become governed Tool Gateway capabilities (approval-required by default); inbound, the assistant is itself an MCP server exposing chat, subject-scoped memory search, and selected capabilities — token-authenticated, role-gated by graph-native exposure rules, fully audited."
license = "Apache-2.0"

[pack.provenance]
authors = ["Yohei Nakajima"]
authored_by = "human"
generator = ""
source_url = "https://github.com/yoheinakajima/activegraph-packs"
created_at = "2026-07-08T00:00:00Z"

[pack.integrity]
content_hash = "sha256:db21672e44e6050d71c5370d63ca3d2a73d14fa24d5245a5a59bd19e3943d12c"

[dependencies]
activegraph = ">=1.9,<2.0"
python = ">=3.11"
python-deps = []

[dependencies.packs]
core = ">=0.1"
tool_gateway = ">=0.1"

[dependencies.optional-packs]
identity_auth = ">=0.1"
chat = ">=0.1"
memory_gateway = ">=0.1"
secrets = ">=0.1"

[surface]
object_types = ["mcp_server", "mcp_exposure", "mcp_access"]
relation_types = []
behaviors = []
tools = []
settings_schema = "MCPSettings"

[[surface.capabilities]]
provider = "mcp"
capability = "set_exposure"
risk_class = "high"
credential_ref = ""
action_class = "R4"

[fixtures]
entrypoint = "fixtures/run_fixtures.py"
deterministic = true
