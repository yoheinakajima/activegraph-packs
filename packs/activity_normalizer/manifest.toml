# Generated by scripts/generate_manifests.py; rerun after editing the
# pack (CI verifies the content hash and the declared surface).

[pack]
name = "activity_normalizer"
version = "0.3.0"
description = "Provider-neutral activity evidence identity, replay, revision, and deterministic candidate extraction."
license = "Apache-2.0"

[pack.provenance]
authors = ["Yohei Nakajima"]
authored_by = "human"
generator = ""
source_url = "https://github.com/yoheinakajima/activegraph-packs"
created_at = "2026-07-08T00:00:00Z"

[pack.integrity]
content_hash = "sha256:8eb7cc1b5be4dae21c0ae769c756c459b4524fcabcebbe19eb4eca54c7ad26c6"

[dependencies]
activegraph = ">=1.9,<2.0"
python = ">=3.11"
python-deps = []

[dependencies.packs]
core = ">=0.1"

[surface]
object_types = ["acquired_item", "acquired_content", "backfill_cursor", "evidence_invalidation_request", "activity_evidence", "ingestion_failure", "extraction_record", "extractor_version_state", "preference_candidate", "task_candidate", "profile_candidate", "skill_candidate", "eval_candidate"]
relation_types = ["content_for", "normalizes_to", "acquired_from", "supersedes", "extraction_for", "produced_candidate", "extracted_from", "failure_for"]
behaviors = ["normalize_acquired_item", "fulfill_evidence_invalidation_request", "publish_cursor_created", "publish_cursor_patch", "migrate_communication_extraction_floor", "select_shared_extraction", "project_structure_candidates"]
tools = ["reextract_evidence", "disable_extractor_version"]
settings_schema = "ActivityNormalizerSettings"

[fixtures]
entrypoint = "fixtures/run_fixtures.py"
deterministic = true
