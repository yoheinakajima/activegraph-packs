# Generated by scripts/generate_manifests.py; rerun after editing the
# pack (CI verifies the content hash and the declared surface).

[pack]
name = "memory_gateway"
version = "0.7.0"
description = "Memory lifecycle manager. Evaluates memory_candidates from Core Pack, accepts high-confidence items into durable MemoryItems, and provides hybrid, outcome-aware retrieval with reversible reliability de-ranking. Default backend: SQLite; external stores (mem0, …) plug in via register_backend()."
license = "Apache-2.0"

[pack.provenance]
authors = ["Yohei Nakajima"]
authored_by = "human"
generator = ""
source_url = "https://github.com/yoheinakajima/activegraph-packs"
created_at = "2026-07-08T00:00:00Z"

[pack.integrity]
content_hash = "sha256:4ab7adad7f849f49c52570a0d51b38cf972922e795b8c2abaecfe49d4ec2f878"

[dependencies]
activegraph = ">=1.9,<2.0"
python = ">=3.11"
python-deps = []

[dependencies.packs]
core = ">=0.1"

[dependencies.optional-packs]
tool_gateway = ">=0.1"
eval_outcome = ">=0.1"

[surface]
object_types = ["memory_item", "memory_promotion_proposal", "memory_retrieval_request", "memory_retrieval", "memory_ranking"]
relation_types = ["promotion_proposal_for", "accepted_as", "fulfilled_by", "ranked_in", "scored_by"]
behaviors = ["candidate_evaluator", "memory_writer", "memory_retriever", "memory_ranker", "memory_reliability_applier", "memory_promotion_proposer"]
tools = ["retrieve_memories"]
settings_schema = "MemoryGatewaySettings"

[fixtures]
entrypoint = "fixtures/run_fixtures.py"
deterministic = true
