# Generated by scripts/generate_manifests.py; rerun after editing the
# pack (CI verifies the content hash and the declared surface).

[pack]
name = "memory_gateway"
version = "0.4.0"
description = "Memory lifecycle manager. Evaluates memory_candidates from Core Pack, accepts high-confidence items into durable MemoryItems, and provides hybrid (lexical + optional embedding) retrieval. Default backend: SQLite; external stores (mem0, …) plug in via register_backend()."
license = "Apache-2.0"

[pack.provenance]
authors = ["Yohei Nakajima"]
authored_by = "human"
generator = ""
source_url = "https://github.com/yoheinakajima/activegraph-packs"
created_at = "2026-07-08T00:00:00Z"

[pack.integrity]
content_hash = "sha256:b3138d530091bf5a657099dbeac9f28572e3245152a5b084b6101a48f630fd1c"

[dependencies]
activegraph = ">=1.4,<2.0"
python = ">=3.11"
python-deps = []

[dependencies.packs]
core = ">=0.1"

[dependencies.optional-packs]
tool_gateway = ">=0.1"

[surface]
object_types = ["memory_item", "memory_retrieval_request", "memory_retrieval", "memory_ranking"]
relation_types = ["accepted_as", "fulfilled_by", "ranked_in", "scored_by"]
behaviors = ["candidate_evaluator", "memory_writer", "memory_retriever", "memory_ranker"]
tools = ["retrieve_memories"]
settings_schema = "MemoryGatewaySettings"

[fixtures]
entrypoint = "fixtures/run_fixtures.py"
deterministic = true
