# Generated by scripts/generate_manifests.py; rerun after editing the
# pack (CI verifies the content hash and the declared surface).

[pack]
name = "public_presence"
version = "0.1.0"
description = "Budgeted, gateway-routed R0 fetching of the owner's public handles; results land as injection-scanned, replay-retained evidence on the public_presence surface."
license = "Apache-2.0"

[pack.provenance]
authors = ["Yohei Nakajima"]
authored_by = "human"
generator = ""
source_url = "https://github.com/yoheinakajima/activegraph-packs"
created_at = "2026-07-08T00:00:00Z"

[pack.integrity]
content_hash = "sha256:a9eb7a0a7605af4133f8bcb21660e96602758abe0477e5ddf63f5c1e41036654"

[dependencies]
activegraph = ">=1.9,<2.0"
python = ">=3.11"
python-deps = []

[dependencies.packs]
activity_normalizer = ">=0.1"
tool_gateway = ">=0.1"

[surface]
object_types = ["presence_bootstrap_run"]
relation_types = ["bootstrap_call"]
behaviors = ["acquire_presence_result"]
tools = ["bootstrap_public_presence"]
settings_schema = "PublicPresenceSettings"

[[surface.capabilities]]
provider = "public_presence"
capability = "fetch_page"
risk_class = "low"
credential_ref = ""
action_class = "R0"

[fixtures]
entrypoint = "fixtures/run_fixtures.py"
deterministic = true
