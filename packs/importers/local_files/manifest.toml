# Generated by scripts/generate_manifests.py; rerun after editing the
# pack (CI verifies the content hash and the declared surface).

[pack]
name = "local_files"
version = "0.1.0"
description = "Bounded, deterministic directory snapshots for UTF-8 text, Markdown, and JSON, emitted through Activity Normalizer's acquired-item contract."
license = "Apache-2.0"

[pack.provenance]
authors = ["Yohei Nakajima"]
authored_by = "human"
generator = ""
source_url = "https://github.com/yoheinakajima/activegraph-packs"
created_at = "2026-07-08T00:00:00Z"

[pack.integrity]
content_hash = "sha256:1368864382e0d34740a952ab511ebc46b5311c777f50f8a667f15c46dcb8c69e"

[dependencies]
activegraph = ">=1.7.1,<2.0"
python = ">=3.11"
python-deps = []

[dependencies.packs]
activity_normalizer = ">=0.1"

[surface]
object_types = []
relation_types = []
behaviors = []
tools = ["import_local_files"]
settings_schema = "LocalFilesSettings"

[fixtures]
entrypoint = "fixtures/run_fixtures.py"
deterministic = true
