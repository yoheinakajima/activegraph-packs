# Changelog

## v0.1.0 — Initial release

- Added bounded, sorted acquisition for UTF-8 text, Markdown, and JSON files.
- Added inline, content-addressed artifact, and reference-only replay modes.
- Added per-item stable cursor progress and explicit acquisition failures.
- Added deterministic, keyless fixtures for valid, malformed, bounded, and
  replay-policy paths.
