# Generated by scripts/generate_manifests.py; rerun after editing the
# pack (CI verifies the content hash and the declared surface).

[pack]
name = "evolution"
version = "0.3.0"
description = "Agent-authored packs under governance: static gates, fork trials against replayed history, bundle-hash pins, verified owner approval, quiescent promote adoption, immediate disable. Ships disabled."
license = "Apache-2.0"

[pack.provenance]
authors = ["Yohei Nakajima"]
authored_by = "human"
generator = ""
source_url = "https://github.com/yoheinakajima/activegraph-packs"
created_at = "2026-07-08T00:00:00Z"

[pack.integrity]
content_hash = "sha256:6a285bec6664a10a15c8fc9ea364bee01050833dcd2ce93f05f1e4ebdd708f72"

[dependencies]
activegraph = ">=1.5,<2.0"
python = ">=3.11"
python-deps = []

[dependencies.packs]
core = ">=0.1"
tool_gateway = ">=0.1"
identity_auth = ">=0.1"

[dependencies.optional-packs]
schedule = ">=0.1"
chat = ">=0.1"

[surface]
object_types = ["capability_gap", "mod_proposal", "gate_result", "mod_trial", "mod_promotion", "mod_rollback", "adoption_ticket"]
relation_types = ["proposes_fix_for", "gated_by", "trialed_in", "promoted_as", "rolled_back_by"]
behaviors = ["gap_detector", "proposal_gatekeeper", "promotion_recorder"]
tools = []
settings_schema = "EvolutionSettings"

[[surface.capabilities]]
provider = "evolution"
capability = "adopt_proposal"
risk_class = "critical"
credential_ref = ""

[[surface.capabilities]]
provider = "evolution"
capability = "disable_promotion"
risk_class = "high"
credential_ref = ""

[fixtures]
entrypoint = "fixtures/run_fixtures.py"
deterministic = true
